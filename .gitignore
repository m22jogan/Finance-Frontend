# Node + Build output
node_modules/
dist/
*.tar.gz

# macOS files
.DS_Store

# Local dev environment
.env

# If you had a backend/server previously
server/
drizzle.config.ts
drizzle/
migrations/

# Optional: Ignore Vite config variants (if you don’t want to track them)
vite.config.ts.*
